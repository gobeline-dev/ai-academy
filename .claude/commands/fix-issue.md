---
argument-hint: <issue-number>
---

!`gh issue view $ARGUMENTS`

Investigate and fix the issue above.

1. Trace the bug to its root cause in the codebase
2. Implement the fix with minimal scope — don't refactor unrelated code
3. Verify the fix doesn't break the build (`npm run build`)
4. Summarize what you changed and why
